.DS_Store
*.log
__pycache__/
.coverage

# local overrides / secrets — never commit
CLAUDE.local.md
.claude/settings.local.json
.env
.env.local

# visual-brainstorming companion scratch (superpowers skill)
.superpowers/

# foglamp codebase scan (contains editToken secret)
.foglamp/

# AI agent config + internal session/handoff docs — not for the public repo,
# kept locally only from here on (untracked from GitHub, files stay on disk)
.claude/
docs/handoff/
